# Trying out a simple implementation of in-memory battleships game with .net 8 webapi.
Some comments inside the code are a personal thing and never to be seen in work/production environment.
Also some property access modifiers are public because of mmanual testing. Also this isnt some kind of library to be concerned with using modifiers such as internal etc.